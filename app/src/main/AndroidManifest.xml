<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wiantoro.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.greeting.GreetingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.login.LoginActivity" />
        <activity android:name=".view.change_password.ChangePasswordActivity" />
        <activity android:name=".view.main.MainActivity" />
        <activity android:name=".view.profil_kandidat.ProfilKandidatActivity" />
        <activity android:name=".view.quick_count.QuickCountActivity" />
        <activity android:name=".view.vote.VoteActivity" />
        <activity android:name=".view.vote.ThankYouVoteActivity" />
        <activity android:name=".view.profil_kandidat.IsiProfilKadidatActivity"/>
        <activity android:name=".view.forgot_password.ForgotPasswordActivity"></activity>
    </application>

</manifest>